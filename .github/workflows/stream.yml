
name: Telegram Live Stream

on:
  repository_dispatch:
    types: [start_stream]
  workflow_dispatch:
    inputs:
      video_url:
        description: 'Video URL'
        required: true
      rtmp_url:
        description: 'RTMP URL'
        required: true
      alist_token:
        description: 'Alist Token'
        required: false

jobs:
  stream:
    runs-on: ubuntu-latest
    timeout-minutes: 360 # 最长运行 6 小时

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Install FFmpeg
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg

    - name: Check Connectivity (Debug)
      env:
        VIDEO_URL: ${{ github.event.client_payload.video_url || inputs.video_url }}
      run: |
        echo "🔍 正在检查视频链接连通性..."
        echo "URL: $VIDEO_URL"
        
        # 使用 curl 检查响应头，如果失败打印完整信息以便调试 (解决 530 问题排查)
        # -I: 仅头信息
        # -L: 跟随重定向
        # --fail: 遇到错误码返回非零
        if curl -I -L --fail --connect-timeout 10 "$VIDEO_URL"; then
            echo "✅ 链接可用"
        else
            echo "⚠️  Curl 检查失败，尝试获取详细响应以排查错误 (如 530/404)..."
            curl -v -L --connect-timeout 10 "$VIDEO_URL" || true
            echo "----------------------------------------"
            echo "⚠️ 注意: 如果看到 'Origin DNS Error' (530)，说明 Tunnel 域名失效或 Termux 端服务未运行。"
            echo "----------------------------------------"
        fi

    - name: Start Streaming
      env:
        # ⚡️ 核心修复: 使用 GitHub 表达式 (||) 处理变量
        VIDEO_URL: ${{ github.event.client_payload.video_url || inputs.video_url }}
        RTMP_URL: ${{ github.event.client_payload.rtmp_url || inputs.rtmp_url }}
        ALIST_TOKEN: ${{ github.event.client_payload.alist_token || inputs.alist_token }}
      run: |
        echo "---------------------------------------------------"
        echo "🚀 任务启动确认"
        echo "📡 视频地址: $VIDEO_URL"
        echo "📺 推流目标: RTMP Server"
        echo "---------------------------------------------------"

        if [[ -z "$VIDEO_URL" ]] || [[ -z "$RTMP_URL" ]]; then
          echo "❌ 致命错误: 变量读取失败，地址为空！"
          exit 1
        fi

        # 构建 FFmpeg 命令数组
        CMD=(ffmpeg -re)
        
        # --- 网络抗波动配置 (关键) ---
        CMD+=(-reconnect 1)
        CMD+=(-reconnect_at_eof 1)
        CMD+=(-reconnect_streamed 1)
        CMD+=(-reconnect_on_http_error 4xx,5xx)
        CMD+=(-reconnect_delay_max 30)
        
        # 增加协议白名单
        CMD+=(-protocol_whitelist file,http,https,tcp,tls,crypto)
        
        CMD+=(-rw_timeout 15000000)
        CMD+=(-user_agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64)")

        # 鉴权 Headers (修复 No trailing CRLF warning)
        if [[ -n "$ALIST_TOKEN" ]]; then
           echo "🔑 检测到 Token，已注入 Headers"
           # 确保 Header 格式正确，添加换行符
           CMD+=(-headers $'Authorization: '"$ALIST_TOKEN"$'\r\n')
        fi

        # 输入与输出
        CMD+=(-i "$VIDEO_URL")
        CMD+=(-c:v copy -c:a aac -ar 44100 -b:a 128k -ac 2)
        CMD+=(-f flv "$RTMP_URL")

        echo "▶️ 开始运行 FFmpeg..."
        "${CMD[@]}"
